import { StyleSheet } from "react-native";

export const ButtonStyles = StyleSheet.create({
      button: {
        backgroundColor: '#fff',
        paddingVertical: 12,
        paddingHorizontal: 30,
        borderRadius: 8,
        cursor: 'pointer',
    },
    buttonText: {
        fontWeight: 'bold',
    },
})