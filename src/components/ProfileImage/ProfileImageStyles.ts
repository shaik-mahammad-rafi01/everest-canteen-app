import { StyleSheet } from "react-native";

export const ProfileImageStyles = StyleSheet.create({
    profileImage: {
        height: 40,
        width: 40,
        marginBottom: 5,
        borderRadius:50
    },

})